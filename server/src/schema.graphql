type Query{
    users:[User!]
    user(id:ID!):User

    companies:[Company!]
    company(id:ID!):Company
}


type User {
    id:Int!
    firstName:String!
    lastName:String!
    email:String!
    companies:[Company!]
}

type Company {
    id:Int
    name:String
}